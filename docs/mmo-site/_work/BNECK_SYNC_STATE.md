# 병목 탭 최신화 — 진행 로그

명세 정본 = `BNECK_SYNC_SPEC.md`. **이 파일은 로그이지 신뢰의 근원이 아니다** —
매 틱 현재 위치는 디스크 사실(`git diff`·`grep` 결과·파일 mtime)에서 재유도할 것.

## 페이즈 판

| # | 이름 | 상태 | 비고 |
|---|---|---|---|
| P0 | 명세 박제 | ✅ 완료 (2026-08-18) | 노션 16편 + 정정 이력 전문 대조 → SPEC 에 고정. **이후 노션 재조회 불필요** |
| P1 | 기존 11장 정정 3건 | ✅ 완료 (2026-08-18) | ⑦ 편차 · ⑩ 소켓 호출 · ⑪ 게임루프 CPU |
| P2 | 신규 카드 4장 | ✅ 완료 (2026-08-18) | ⑫GQCSEx ⑬digest ⑭K4K5 ⑮9B · 결론 ⑫→⑯ |
| P3 | 자리 0 네 칸 | ✅ 완료 (2026-08-18) | 현황판(눈금 7,200) · 트래픽 · 결론 · 정정 이력 |
| P4 | 문구·눈금 | ✅ 완료 (2026-08-18) | 12건→16건 · 5,200→7,200 · ringPct |
| P5 | 빌드·검증 | ✅ 완료 (2026-08-18) | CRLF 정규화 → build.ps1 → 육안 |

**한 틱 = 한 페이즈.** 페이즈를 끝내면 이 표를 갱신하고, 아래에 자가검증을 적는다.

---

## P0 — 명세 박제 ✅ 완료 (2026-08-18)

**한 일**
- 노션 DB 「병목 개선」 **16편 전문 + 정정 이력 하위 페이지**를 읽어 수치를 `BNECK_SYNC_SPEC.md`에 고정
- 사이트 `scene.js` `EXPS`·`NOTION_EXP`·`LOAD`·`PINS`, `bneck.js` `SCALE`·`ringPct`, `panel-bneck.html` 구조 파악
- 신규 4장·자리 0 네 칸의 **붙여 넣을 수 있는 초안**까지 작성

**대조 결과**
| 대상 | 결과 |
|---|---|
| 기존 11장 중 ①②③④⑤⑥⑧⑨ | ✅ 노션과 일치 — 손댈 것 없음 |
| ⑦ IOCP 워커 12→4 | ⚠ "3회 편차" 왼쪽 `48~185` → 노션엔 `102 / 185 / 140` |
| ⑩ 송신 워커 K2→K3 | ⚠ 소켓 호출 K2 `99k` → 노션 현재 `100k` (ab + LOAD 두 곳) |
| ⑪ 코어 격리 | ⚠ 게임루프 CPU 절대값이 노션에 생김 (`0.531 → 0.503`) |
| 노션에만 있는 실험 | 4편 (GQCSEx · digest · K4K5 · 9B) |
| 자리 0 네 칸 | 전부 5,200 시대 값 — 전면 갱신 대상 |

**자가검증**
| 항목 | 결과 |
|---|---|
| 신규 4장의 `bm`/`am`/`ab`/`LOAD` 가 SPEC 에 전부 있는가 | ✅ 네 장 모두 |
| 정정 3건의 old → new 가 값까지 적혀 있는가 | ✅ |
| 자리 0 네 칸 초안이 있는가 | ✅ |
| P1~P4 가 노션 없이 수행 가능한가 | ✅ 부록에 원문 수치 채집분 보존 |
| 소스 파일 변경 | **0건** — P0 는 문서만 만든다 |

**내가 새로 만든 것 0** — `_work/` 두 파일 외에 손댄 자리 없음.

**다음 틱** — P1. `scene.js` 세 곳만 고치고 CRLF 정규화까지. 빌드는 하지 않는다.

---

## P1 — 기존 11장 정정 3건 ✅ 완료 (2026-08-18)

**손댄 자리** — `parts/scene.js` 네 군데(정정 3건 · 그중 ⑩은 `ab`와 `LOAD` 두 곳)

| 카드 | 고친 것 | 전 → 후 |
|---|---|---|
| ⑦ IOCP 워커 12→4 | `ab` 3회 편차 | `48~185` → **`102~185`** (+ 경위 주석 2줄) |
| ⑩ 송신 워커 K2→K3 | `ab` 소켓 호출 | `99k` → **`100k`** |
| ⑩ 송신 워커 K2→K3 | `LOAD` 표 · 주석 | `99000` → **`100000`**, 주석의 "3절 표에는 K2 99k" 문장 폐기 |
| ⑪ 코어 격리 | `ab` 게임루프 CPU | `"" / −5% / 절대값은 노션 미기재` → **`0.531 → 0.503 코어 / −5%`** |

⑪의 긴 주석은 전제가 뒤집혀 통째로 갈아썼다 — "노션에도 CSV 에도 없는 수"가 아니라 **지금은 노션이 그 쌍을 싣는다**.
CSV(0.5234→0.5076 · −3.0%)와 다른 이유(3rep 중앙값 대 r1)는 주석에 남겼다.

**자가검증**
| 항목 | 결과 |
|---|---|
| `git diff --stat` 범위 | ✅ `scene.js` 한 파일 · +13 −11 |
| 명세에 없는 자리 변경 | ✅ 0건 — diff 전량이 위 표의 넷 |
| `node --check scene.js` | ✅ syntax OK |
| 옛 값 잔존 (`"99k"` · `99000` · `절대값은 노션 미기재`) | ✅ 0건 (`48~185`는 경위 주석 안에만 — 의도) |
| 인코딩 | ✅ BOM 없음(`2f 2a 20`) · CRLF 2,099 = 총 줄 2,099 (전량 CRLF) |
| 빌드 산출물(`index.html`) 건드림 | ✅ 없음 — P5 몫 |

**내가 새로 만든 것 0** — 손댄 자리 넷 외 변경 없음.

**다음 틱** — P2. 신규 카드 4장(⑫⑬⑭⑮) + `NOTION_EXP` 재배치(결론 ⑫→⑯) + `LOAD` 4행. 명세 P2 절이 붙여 넣을 코드까지 갖고 있다.

---

## P2 — 신규 카드 4장 ✅ 완료 (2026-08-18)

**손댄 자리** — `parts/scene.js` 세 군데 (`+91 −13`)

1. `EXPS` — ⑪ 뒤 · 미측정 3건(`s:21`) 앞에 **`s:12`~`s:15` 삽입**. 앞머리에 "여기부터 넷은 digest 이후" 묶음 주석
2. `NOTION_EXP` — 4행 추가, 결론 행이 **⑫ → ⑯**으로 밀림
3. `LOAD` — `[13]` `[14]` `[15]` 추가 (⑫는 행 없음 · 사유를 주석에 남김)

| 카드 | s · loc · 판정 | 배지 동접 | 특이 |
|---|---|---|---|
| ⑫ 완료 통지 묶기 | 12 · 자리1(수신 길목) · 기각 | 4,800 | `LOAD` 행 없음 · `bm:null`(tick은 양팔 공통 게이트) |
| ⑬ 멤버십 통보도 digest 로 | 13 · 자리2(틱 예산) · 채택 | 5,600 | 여섯 칸 전부 있음 |
| ⑭ 송신 워커 K4·K5 | 14 · 자리3(송신 경로) · 기각 | 5,600 | `tick:null`(노션이 평균만 적음) · `ab` 5줄 |
| ⑮ 좌표를 눈금으로 (9B) | 15 · **자리4(회선 상한)** · 채택 | 5,600 | `tick:null` · `ab` 5줄 · 자리 선택 근거를 주석에 남김 |

**자가검증**
| 항목 | 결과 |
|---|---|
| `node --check scene.js` | ✅ syntax OK |
| `EXPS` 의 `s` 중복 | ✅ 없음 — `0 0.1 0.2 0.3 1~15 21 22 23` |
| `NOTION_EXP` 길이 = 카드 수 | ✅ **16행** (`1~15` + `0.2`) → 번호 ①~⑯ 연속 |
| `LOAD` 의 `s` 가 전부 `EXPS` 에 있는가 | ✅ 15행 · ⑫만 의도적으로 빠짐 |
| 핀 부제 검산 (`loc` 실측) | ✅ 자리1 `채택1·기각1·병목아님2` / 자리2 `채택5·기각2` / 자리3 `채택2·기각2` / 자리4 `채택1·진단1` / 자리5 `병목아님1` |
| 계기판 곱셈 정합(송신 메시지 × 평균 패킷 = 송신량) | ✅ ⑬ 1,305만×95.0=1,240 · ⑮ 1,299만×74.6=969 |
| 인코딩 | ✅ BOM 없음 · CRLF 2,175 = 총 줄 2,175 |
| 빌드 산출물 건드림 | ✅ 없음 |

**넘긴 것 두 가지 (P3에서 닫는다)**
- `NOTION_EXP` 주석에는 결론을 **"세트 복제로"** 라 적었는데 `EXPS` 의 `n` 은 아직 `"결론 — 수평 확장으로"` 다.
- `LOAD` 의 `[0.2]` 행은 새 값(`1037 / 74.7 / 136000`)으로 이미 바꿨다 — 표를 한 번에 손대는 편이 안전해서다.
  `EXPS` 의 `s:0.2` 본문은 아직 옛 5,000 기준이라, **P3 전까지 ⑯ 카드만 부하 줄과 본문 기준이 어긋나 있다.**

**내가 새로 만든 것 0** — 위 세 군데 외 변경 없음. 명세에 없는 값은 넣지 않았다.

**다음 틱** — P3. 자리 0 네 칸(현황판·동접별 트래픽·결론·정정 이력). 현황판 `am.ccu` 가 눈금 7,200이 되는 순간이라, 넘치는 칸(`over`)이 생기는지 P5 육안에서 볼 것.

---

## P3 — 자리 0 네 칸 ✅ 완료 (2026-08-18)

**손댄 자리** — `parts/scene.js` 자리 0 블록 전체 + 그 앞 묶음 주석

| 칸 | 무엇이 바뀌었나 |
|---|---|
| s:0 **현황판** | `~5,000 / ~5,200` → **한계 7,200 · 정격 5,500 · 운용 5,000**. 판정 기준 2개 → **4개**(세션 도달·재접속 0 추가). 병목을 **게임 루프 → 송신계**로. `am` 이 눈금 7,200을 공급 |
| s:0.1 **동접별 트래픽** | 제곱 명제는 유지, **옛 6,000/6,500 붕괴 사다리 폐기**(오염 회차) → 14B·9B 천장 사다리로 교체 |
| s:0.2 **결론(⑯ 카드)** | 이름 `수평 확장으로` → **`세트 복제로`**. `맵당 3.4코어 · ~1,470명` → **세트 2.4코어 · ~2,080명**. `tick` 을 39.7 → **null**(버킷 보간이라 정밀 인용 금지) · 대신 틱 평균 10.4ms 를 `ab` 로 |
| s:0.3 **정정 이력** | 5항목 → **7항목**(§6 환경 오염 · §7 재측정 종결 추가). K4 줄은 근거 교체(`재현 안 됨` → `금지 아님 · 이득도 없음`), 병목 연쇄 `5단계` → `7단계` |

**되돌린 실수 1건 (기록)** — s:0.1 을 고치면서 `ex:` 한 줄을 교체문에서 빠뜨렸다가 즉시 복구했다.
네 칸 전부 `ex` 가 있는지 `awk` 로 재확인(1166 · 1186 · 1204 · 1227행).

**자가검증**
| 항목 | 결과 |
|---|---|
| `node --check scene.js` | ✅ syntax OK |
| 자리 0 네 칸의 `r/cond/m/ex/abh` 필드 | ✅ 전부 존재 (`ex` 누락 복구 확인) |
| 옛 5,000 시대 값 잔존 | ✅ 0건 — `1,470명`·`맵당 3.4코어`·`10130000`·`6,500 붕괴`·`5단계 연쇄` 전부 사라짐 (남은 1곳은 경위를 적은 주석 안) |
| `SCALE` 공급값 | ✅ `ccu 7200` · `pps 12,980,000` · `send 969` · `pkt 74.7` · `sock 136,000` · `tick 40`(상수) |
| 인코딩 | ✅ BOM 없음 · CRLF 2,193 = 총 줄 2,193 |

**P5 육안에서 반드시 볼 것 두 가지 (P3에서 산술로 미리 잡음)**
1. **고리는 어느 카드도 꽉 차지 않는다** — 배지 동접 최대가 ⑬⑭⑮의 5,600이고 눈금이 7,200이라 **78%**다.
   7,200은 천장 값이지 A/B 부하점이 아니라서 그렇다. 명세의 "⑮ 고리 꽉 참" 문구는 이 실측으로 정정했다.
2. **`over` 배지가 여러 칸에 붙는다** — 눈금이 9B 이후 값이라 14B 시절 카드(⑪ 송신량 1,006 · ⑬⑭ 1,240~1,249)가
   자연히 넘친다. 문제는 **⑮ 송신량 970 대 눈금 969** — 0.1% 차이인데 문턱(1.001)을 넘어 배지가 붙는다.
   같은 값의 반올림 차이라 그림이 지저분하면 손볼 것. 969는 노션 결론 페이지 값이라 임의로 못 고친다.

**내가 새로 만든 것 0** — 자리 0 블록 외 변경 없음.

**다음 틱** — P4. 문구·눈금(`12건`→`16건` · `5,200`→`7,200` · `열두`→`열여섯` · `ringPct` 기본값). 세 파일 전부 손댄다.

---

## P4 — 문구·눈금 ✅ 완료 (2026-08-18)

**손댄 자리** — 세 파일 (누계 `+212 −103`)

| 파일 | 고친 것 |
|---|---|
| `panel-bneck.html` | **라벨 `실험 16건 · 동접 200 → 7,200`** · `aria-label` 16건 · 구조 주석 · ↗ 개수 문구 |
| `bneck.js` | 머리 주석 · `MET` 곱셈 예시 · `SCALE` 주석 · 카드 주석 · `paintFan` hint 목록 · **`ringPct` 기본값 5000 → 7200** |
| `scene.js` | `EXPS` 머리 주석 · `NOTION_EXP` 앞 주석(열둘→열여섯, 5,200→7,200) · `LOAD` 앞 주석 · `PINS` 자리 0 `why` · 자리 2 앞 주석 · 핀 클릭 폐지 주석 |

**말을 바꾼 것이 아니라 사실이 바뀐 곳 셋** — 단순 치환으로 끝나지 않아 따로 적는다.

1. `bneck.js` **눈금 주석** — "① 은 거의 비어 있고 ⑪ 은 꽉 찬다"가 이제 거짓이다(꽉 차는 카드 없음).
   "뒤로 갈수록 찬다"로 바꾸고, **옛 카드가 넘치는 것이 정상인 이유**(9B 로 바이트를 깎은 것이 지금이라
   14B 시절 카드가 눈금 969를 넘는다)를 새 문단으로 남겼다.
2. `bneck.js` **`ringPct` 주석** — 고리가 꽉 차는 카드가 없는 사정(눈금 7,200은 천장, 카드는 A/B 부하점 5,600 = 78%)을 적었다.
3. `scene.js` **핀 자리 2 주석** — "여섯 장이 여기 선다 … 이 페이지의 결론('병목은 게임 루프 단일 코어')"이
   ⑬ 이후로 틀렸다. **일곱 장**으로 고치고, 이 자리의 두께가 이제 "지금 병목"이 아니라
   **"가장 오래 병목이던 곳"**을 뜻한다고 밝혔다.

**자가검증**
| 항목 | 결과 |
|---|---|
| `node --check` scene.js · bneck.js | ✅ 둘 다 syntax OK |
| `열두`·`열둘`·`12건` 잔존 | ✅ 화면 문구 0건 (`panel-bneck.html` 2곳은 레일 시절 경위 서술 — **"그때 열둘"**로 시점을 박아 유지) |
| `5,200` 잔존 | ✅ 전부 정당 — ⑪의 부하점(5곳) · ⑬의 "천장 5,200→6,600"(2곳) · 정정 이력 사슬(1곳) · `LOAD` 숫자 `135200`(우연 매칭 2곳) |
| `ringPct` 기본값 | ✅ `SCALE.ccu \|\| 7200` |
| 인코딩 3파일 | ✅ BOM 없음 · CRLF = 총 줄 수 (2,196 / 376 / 65) |
| 빌드 산출물 건드림 | ✅ 없음 |

**내가 새로 만든 것 0** — 명세 P4 표의 자리 외에 손댄 곳 없다. 값은 하나도 바꾸지 않았다(문구·주석·기본값뿐).

**다음 틱** — P5(마지막). `build.ps1` 풀 빌드 → 헤드리스 캡처 육안. 볼 것은 카드 16장·번호 ①~⑯·⑯ 계기판,
그리고 P3에서 예고한 **`over` 배지 밀도**(특히 ⑮ 970 대 눈금 969)다. 끝나면 루프를 종료한다.

---

## P5 — 빌드·검증 ✅ 완료 (2026-08-18) · **캠페인 종료**

**한 일**
1. 세 조각 CRLF 정규화 → `build.ps1 -NoSync` 풀 빌드 (`index.html` 841 KB · 폰트 서브셋 정상)
   - `-NoSync` 를 쓴 이유 = 풀 동기는 `sync-scene.ps1` 까지 불러 `portfolio.html` 을 건드리는데,
     그쪽은 렌더 결과라 같은 입력에도 값이 흔들린다(`parts/README.md`). 이번 변경 범위 밖이다.
2. 헤드리스 크롬 DOM 덤프로 카드 목록 전수 확인
3. 스크린샷 육안 → **이름 잘림 2건 발견 → 고치고 재빌드·재캡처**

**DOM 실측 (헤드리스 `--dump-dom`)**
| 항목 | 결과 |
|---|---|
| 카드 번호·판정·이름 | ✅ ①~⑯ 연속 · 판정 배지 전부 정상 |
| 라벨 줄 | ✅ `실험 16건 · 동접 200 → 7,200 으로 병목이 옮겨간 순서` |
| 고리 % (16장) | ✅ `3 3 14 14 12 28 56 56 56 69 72 67 78 78 78 78` — 계산과 일치, **꽉 찬 카드 없음**(최대 78%) |
| 핀 부제 | ✅ 수신 길목 `채택1·기각1·병목아님2` / 틱 예산 `채택5·기각2` / 송신 경로 `채택2·기각2` / 회선 상한 `채택1·진단1` / 저장 경로 `병목아님1` |

**육안에서 잡은 결함 1건 (고침)** — 카드 폭에서 이름 두 개가 잘렸다.
- `멤버십 통보도 digest 로` → **`멤버십 digest`** (⑧ `멤버십 팬아웃 묶음` 과 짝)
- `좌표를 눈금으로 (9B)` → **`좌표를 눈금으로`** (9B 는 성과 줄·계기판이 말한다)
재빌드 후 재캡처로 둘 다 온전히 표시되는 것을 확인했다.

**자가검증**
| 항목 | 결과 |
|---|---|
| 빌드 | ✅ exit 0 · include 마커 전부 치환 · 고아 조각 0 |
| `git status` | ✅ `index.html` + 조각 3개만 `M` (`_work/` 는 신규) |
| 누계 diff | `index.html` +320/−? · `scene.js` +244 · `bneck.js` +52 · `panel-bneck.html` +22 |
| 헤드리스 임시 프로필 | ✅ 정리함 |

**미검증으로 남긴 것 (정직)**
- **카드를 골랐을 때의 계기판**은 헤드리스로 못 봤다. 가상시간에서 `setTimeout` 클릭이 반영되지 않고
  숫자가 구르는 중간 프레임으로 찍힌다(알려진 한계). 기본 선택인 ① 계기판은 정상 렌더를 확인했다.
- 따라서 **`over` 배지는 산술 판정으로만 확정**했다 — ⑮ 송신량 `970 / 969 = 1.00103` 이 문턱 `1.001` 을
  넘어 **배지가 붙는다**. 0.1% 차이라 화면에서 거슬리면 손볼 자리이고, 판단은 주우진 몫이다.

**명세 범위 밖 발견 1건 (안 고침)**
- 페이지 헤더 부제가 **`동접 5,000 · 구조와 병목`** 이다(셸 `mmo.src.html`). 7,200 시대와 어긋나 보이지만
  5,000은 여전히 **맵당 운용값**으로 참이라 틀린 문구는 아니다. 사이트 첫인상이라 임의로 안 고치고 넘긴다.

**내가 새로 만든 것 0** — 빌드 산출물(`index.html`)과 이름 두 개 외에 P5 에서 손댄 자리 없다.

---

# ✅ 캠페인 종료 (2026-08-18)

P0~P5 전부 완료. 노션 「병목 개선」 16편과 사이트 `#bneck` 탭이 맞았다.

---

## 자가검증 (2026-08-18 · 요청)

범위 = **이번에 손댄 자리**. 기준 = "결함 0" 이 아니라 **"내가 새로 만든 것 0"**.

**결과 — 새로 만든 것 4건 발견, 전부 고쳤다.** 셋은 내 변경이 남의 문장을 낡게 만든 것이고,
하나는 내가 고른 데이터 모양이 화면 문구와 어긋난 것이다.

| # | 무엇 | 왜 내가 만든 것인가 | 조치 |
|---|---|---|---|
| 1 | `parts/bneck.css:157` 카드 이름 폭 규칙 주석 — "열두 줄" · **"가장 긴 이름은 '결론 — 수평 확장으로'"** | 내가 그 이름을 `세트 복제로` 로 바꿔 근거가 낡았다. 실제로 P5 에서 이름 잘림이 났던 바로 그 규칙이다 | 열여섯으로 고치고, **잘렸던 두 이름과 그 대처를 규칙에 기록**(다음에 카드를 더할 때 볼 자리) |
| 2 | `mmo.src.html:85` 셸 주석 — **"1-2 의 '실험 12건 …' 을 따온 것"** | 내가 그 라벨을 `16건` 으로 바꿔 지칭이 어긋났다 | 숫자를 빼고 `실험 N건 …` 으로 — 다시 늘어도 안 낡는다 |
| 3 | ⑫ 계기판 **틱 칸** — 값은 `39.2ms` 인데 왼쪽 문구가 `이 시기 미수집` | 내가 `am.tick=39.2` · `bm:null` 로 넣어서 생긴 모순. 39.2 는 A/B 값이 아니라 **양팔 공통 게이트**라 계기판에 세울 값이 아니었다 | `am.tick=null` 로 내리고, 값은 `ab` 에 **"틱 p99 (게이트) · 양팔 공통"** 으로 보존 |
| 4 | ⑯ 계기판 **틱 칸** — 값이 `—` 인데 왼쪽 문구가 `지금 값` | 내가 `tick` 을 39.7 → null 로 바꿔 생긴 조합(버킷 보간이라 인용 금지). '지금 값이 없다' 로 읽힌다 | `bneck.js paintDash` 한 줄 — `sum` 이면서 값이 없으면 왼쪽을 비운다 |

**고친 뒤 재검증**
| 항목 | 결과 |
|---|---|
| `node --check` scene.js · bneck.js | ✅ syntax OK |
| 재빌드 | ✅ `index.html` 843 KB |
| 산출물에 낡은 지칭 | ✅ `실험 12건` 0건 · `수평 확장으로` 0건 (고치기 전에는 각 1건 있었다) |
| 새 값 반영 | ✅ `틱 p99 (게이트)` · `av==null ? "" : "지금 값"` 각 1건 |
| 인코딩 4파일 | ✅ BOM 없음 · CRLF = 총 줄 수 (2,202 / 379 / 217 / 180) |
| **설계 탭(1-1) 회귀** | ✅ 정상 렌더 — `scene.js` 를 공유하지만 씬·칩·패널 이상 없음 |
| 병목 탭 재캡처 | ✅ 카드 16장 · 이름 잘림 없음 · 라벨 정상 |

**여전히 미검증** — 카드를 **골랐을 때의** 계기판. 헤드리스 가상시간에서 클릭이 반영되지 않아
③④ 의 수정 결과를 화면으로 확인하지 못했다(코드·데이터로는 확정). 실제 브라우저에서 ⑫·⑯ 을
눌러 보면 바로 확인된다.

---

## 자가검증 2차 — 실제 브라우저 (2026-08-18)

헤드리스로 못 본 것을 실제 크롬으로 봤다(`python -m http.server 8765` → `localhost:8765/index.html#bneck`).
`file://` 은 확장이 접근하지 못해 로컬 서버를 띄웠다.

**여기서 2건이 더 나왔다 — 둘 다 화면을 봐야만 보이는 것이었다.**

| # | 무엇 | 원인 | 조치 |
|---|---|---|---|
| 5 | ⑫ **동접 칸**이 `4,800명` 인데 왼쪽이 `이 시기 미수집` | `bm:null` + 판정 `기각` 조합을 내가 처음 만들었다. 그 조합은 `paintDash` 의 마지막 fallback 으로 떨어진다(⑤ 는 `dg` 라 전용 분기가 있었다) | `bneck.js` — `av!=null` 이면 `이 부하에서 관측`. **단 `dg`·`sum`·`ld` 뒤에** 둔다 |
| 6 | **카드 이름이 대거 잘림** (넓은 화면에서만) | 판이 `grid-auto-flow:column` 이라 **줄 수가 열 수를 정한다**. 줄 4 · 카드 16 → ⌈16/4⌉ = **네 열**(212px). 12장 시절엔 세 열(273px)이었다 | `bneck.css` — 줄 `4 → 6` (⌈16/6⌉ = 세 열). CSS 주석이 이미 예고한 자리였다("15 건이 되면 5 로") — 다만 16 건이라 5 로는 모자란다 |

**고치다 회귀 1건을 만들었고, 대조군에서 잡았다** — ⑤ 의 `dg` 분기를 없애고 일반화했더니
② 평균 패킷이 `이 실험의 부하` → `이 부하에서 관측` 으로 바뀌었다. 분기 순서를 되돌려
기존 카드 문구를 전부 원래대로 유지하면서 ⑫ 만 고쳐지게 했다.

**브라우저 실측 (수정 후 · 뷰포트 1797)**
| 항목 | 결과 |
|---|---|
| 카드 수 · 열 | ✅ 16장 · **3열** (카드 257px) |
| 잘린 이름 (`scrollWidth > clientWidth`) | ✅ **0건** |
| ⑫ 계기판 | ✅ 동접 `4,800명 / 이 부하에서 관측` · 없는 값은 `이 시기 미수집`·`기록 없음` |
| ⑯ 계기판 | ✅ 틱 `— / (빈 문구)` · 나머지 다섯 칸 `지금 값` |
| ② 대조군 | ✅ `82.1k →` 등 A/B 화살표 · 평균 패킷 `이 실험의 부하` (원래대로) |
| ⑤ 대조군 | ✅ 전 칸 `이 부하에서 관측` (원래대로) |
| ⑮ `over` 배지 | ✅ 소켓 호출 칸에 붙는다(136.2k > 눈금 136.0k) — 예상대로 미세 초과 |

**남은 관측 1건 (안 고침)** — 판 높이 613px 대 지도 574px 로 **판이 39px 더 길다**.
줄을 6으로 올린 대가다. CSS 주석이 경계하던 "판이 지도보다 길어지는" 상태이긴 한데,
화면에서는 어색하지 않아 손대지 않았다. 줄이려면 카드 높이·gap 이나 계기판을 만져야 한다.

**자가검증 총계 — 내가 새로 만든 것 6건, 전부 고쳤다.** 그중 2건은 실제 브라우저에서만 보였다.

---

## 판 재설계 검토 — ❌ 기각, 원상 유지 (2026-08-18)

카드가 16장이 되며 "판 디자인을 새로 잡을까" 를 검토했다. **여덟 안을 실제로 렌더해 보고 전부 기각.**
주우진 판단 = **"안 건든 게 제일 낫다."**

| 안 | 무엇 | 왜 안 됐나 |
|---|---|---|
| B | 카드 2열 + 계기판 가로 1행 | 카드 479px 중 이름 뒤 **342px 가 빈다** |
| C | 카드 2열 + 계기판 세로 1열 | 막대는 534px 로 시원해지나 **지도 아래가 247px 빈다** |
| D | 계기판도 2열 | 세로선은 맞지만 계기판 막대가 **100px 로 죽는다** |
| E | 카드 1열 16행 | 사다리는 이어지나 **계기판이 화면 밖으로 밀린다** |
| F | 3단 [그림][지표][카드] | **고르기 전엔 빈 숫자가 가운데를 차지**하고 세 덩이 높이가 제각각 |
| G·H | 카드 이름 뒤에 동접 막대 | 목록이 사다리 그래프가 되지만, 2열이라 **사다리가 두 토막**이고 계기판과 상충 |

**남은 판단** — 지금 판(카드 3열 6행 · 계기판 3칸 2행)이 하는 일은 이미 온전하다.
판이 지도보다 39px 긴 것은 **그 대가로 배치를 흔들 만한 값이 아니다.**
다음에 카드를 더할 때 다시 걸릴 자리는 `bneck.css` 의 줄 수 주석에 규칙으로 남겨 뒀다
("세 열이 유지되는 최소 줄 수").

**정리** — 시안 이미지·비교 페이지 전량 삭제, 임시 스타일은 브라우저 메모리에만 있었으므로 흔적 없음.
로컬 서버 종료. **CSS 실효 변경은 `#bnc-b` 줄 수 `4 → 6` 하나뿐이고, 그것은 재설계가 아니라
16장에서 이름이 잘리던 것을 고친 자가검증 결과다.**

---

## 수치 전수 재대조 (2026-08-18 · 요청 "절대 틀리면 안 돼")

**노션 16편 + 정정 이력을 다시 읽어** 지금 파일에 든 값과 항목별로 맞췄다. 명세(P0 채집분)를 믿지 않고
원본을 다시 당긴 이유는 그 사이 노션이 바뀌었을 수 있어서인데, **실제로 한 편이 바뀌어 있었다.**

| 대상 | 결과 |
|---|---|
| ①~⑪ 기존 11장 | ✅ **전부 일치** — `bm`/`am`/`ab`/`LOAD`/`cond` 전 항목. P1에서 고친 ⑦·⑩·⑪ 세 값도 노션과 정확히 맞다 |
| ⑫ GQCSEx · ⑬ digest · ⑮ 9B | ✅ 일치 |
| ⑭ K4·K5 | ⚠ **Δ 표기 3건 어긋남** |
| ⑯ 결론 (노션 결론 페이지) | ⚠ **오늘 개편됨** (08-18 12:41) — 문구 2건 영향 |
| 정정 이력 7항목 | ✅ 일치 |

**고친 것 (총 5건 · 전부 이번 대조에서 처음 발견)**

| # | 자리 | 전 → 후 | 왜 |
|---|---|---|---|
| 1 | ⑭ 송신량 Δ | `±0%` → **`+0.7%`** | 노션 표는 `+0.7%`. 내가 본문의 "평평"이라는 **서술**을 옮겨 적었다 |
| 2 | ⑭ syscall당 바이트 Δ | `±0%` → **`−0.4%`** | 같은 이유 |
| 3 | ⑭ 틱 평균 Δ | `±0%` → **`K5 는 11.4`** | 노션이 Δ 를 안 준 칸이다(11.7 → 12.2 → 11.4 로 오르내려 방향이 없다). 없는 Δ 를 만들지 않고 요동 자체를 보인다 |
| 4 | ⑯ 결론 `r` | `근본 벽은 팬아웃` → **`브로드캐스트`** | 노션 🎯 콜아웃이 오늘 그 말로 바뀌었다. 같은 것을 가리키지만 정본이 고른 말을 따른다 |
| 5 | 정정 이력 `병목 위치` | `7단계 연쇄` → **`단계마다 옮겨감`** | 그 숫자는 결론 페이지의 '이동 경로' 목록을 센 것인데, 개편에서 그 목록이 빠져 **셀 근거가 사라졌다** |

**자가검증**
| 항목 | 결과 |
|---|---|
| 대조 범위 | ✅ 노션 16편 + 정정 이력 = **17개 페이지 전문** |
| `node --check` · 재빌드 | ✅ syntax OK · `index.html` 845 KB |
| 값을 임의로 만든 곳 | ✅ 0건 — ⑭ 틱은 Δ 가 없어 **비웠고**, 정정 이력은 숫자를 **뺐다** |
| 대조 근거 | 각 페이지 본문 표를 그대로 인용. 캐시 시각(`as of`)으로 변경 여부까지 확인 |

**남는 주의 하나** — 노션 결론 페이지는 지금도 손질 중이다(오늘만 한 번 바뀌었다). 이 카드(⑯)와
자리 0 네 칸은 그 페이지를 그대로 따르므로, **결론 페이지를 고치면 여기도 같이 봐야 한다.**

**남은 것은 주우진 판단 2건**(헤더 부제 · ⑮ `over` 배지)과 **push** 뿐이다.

---

## P7 — 빈칸 채우기 자가검증 (2026-08-19)

기준은 "결함 0" 이 아니라 **"내가 새로 만든 것 0"**. 범위는 이번에 손댄 자리(⑭ ⑮ ⑯ · 현황판 · 동접별 트래픽 · SCALE)뿐이다.

| # | 내가 만든 것 | 어떻게 드러났나 | 처리 |
|---|---|---|---|
| 1 | 눈금이 **반만** 7,200 이 됐다 | 현황판 am 을 7,200 으로 올렸는데 SCALE.pkt/sock 은 결론 카드(5,600)에서 오고 있었다 | LOAD 에 `[0, …]` 행을 세우고 SCALE 을 NOWE 우선으로 — 여섯 칸이 한 지점 |
| 2 | ⑭ 계기판이 자기모순 | 평균 패킷(94.9)을 채우자 `1,305만 × 94.9 = 1,239` 인데 옆 칸은 `1,249` | am.pps 를 K4 실측 1,315.5만 으로 — 곱셈이 닫힘 |
| 3 | ⑭ 와 ⑮ 가 **같은 런을 다른 숫자로** 말함 | 둘의 '끄고' 는 같은 `RMB_…_CC5600` 인데 소켓 135.1k vs 135.2k · pps 1,306만 vs 1,305만 | ⑮ 를 ⑭ 쪽(CSV)으로 통일 |
| 4 | 현황판 ab 헤더만 7,200 | 헤더를 7,200 으로 고쳤는데 "코어당 수용" 한 줄은 운용 5,000 기준 | 라벨에 기준을 박음 |

### 검산 (전 카드)
- **곱셈 정합** `송신 메시지 × 평균 패킷 = 송신량` — 값이 셋 다 있는 모든 팔에서 오차 ≤0.1%(반올림 범위).
- **빈칸** — 16 장 × 6 칸 전수 순회, "이 시기 미수집" 0 · "—" 0.
- **막대** — 눈금이 커졌는데도 100% 초과 0.

### 하마터면 틀릴 뻔한 것
CSV 지표 이름은 `tick_p99` 가 아니라 **`tick_p99_ms`** 다. 정확 일치로 물었더니 "없음" 이 나와 값의 출처가 흔들리는 줄 알았다.
다시 물어 확인 : RMB 19.88 · RMC4 19.90 · RMD 19.87 · RMDU(7,200 유효 2rep) 39.75 — 화면에 넣은 19.9 / 39.7 이 맞다.

### 남은 빈칸 (고칠 수 없는 것)
⑤ 회선 상한의 **소켓 호출** 한 칸. 붕괴 평형 구간이라 CSV 에도 원본이 없고 노션도 그렇게 적었다.

---

## 검사기 도입 (2026-08-19) — 자가검증을 스크립트로 대체

**앞으로 "검증해" 는 `node _work/verify.mjs` 한 줄이다.** 눈으로 대조하는 방식은 그만둔다 —
회차마다 기준이 달라서 매번 새로운 종류의 결함이 나왔고, 특히 **값끼리의 관계**가 깨진 것은
노션 전수 대조로도 안 잡혔다(⑭·⑮ 가 같은 런을 다른 숫자로 말하던 일). 기준을 규칙으로 박는다.

### 넣은 것 둘 (값은 하나도 안 고쳤다)

| 무엇 | 어디 |
|---|---|
| 출처를 데이터로 승격 — 카드마다 `src:{bm,am}` · 예외 칸은 `srcOf` · 부하 표는 `LOAD_SRC` | `parts/scene.js` (+59줄, 값 변경 0) |
| 검사기 | `_work/verify.mjs` |

- 주석은 그대로 뒀다. **근거를 설명하는 것은 여전히 주석**이고, `src` 는 그 주석이 이미 적어 둔
  런 라벨·페이지 id 만 기계가 물어볼 수 있게 꺼낸 것이다.
- 라벨 문법 — `RMB_…_r1` 그 rep 하나 / `RMB_…` 그 런의 rep 전체 평균 / `A_r1+B_r1` 고른 rep 평균 /
  `notion#끄고` 이 카드의 노션 보고서 끄고 열 / `notion:<id>#<절>` 다른 페이지.
  **같은 문자열을 가리키는 값끼리는 같아야 한다** — 이것이 R3 이 서는 자리다.

### 규칙

| ID | 규칙 | 첫 실행 |
|---|---|---|
| R0 | 검사기가 보는 자리(계기판 여섯 칸 · 라벨 문구 · 눈금 배선)가 그대로인가 | 8건 · 위반 0 |
| R1 | 계기판에 서는 모든 값에 출처가 있는가 (+ 그 런이 CSV 에 실재하는가) | 183건 · 위반 0 |
| R2 | 송신 메시지 × 평균 패킷 = 송신량 (≤0.5%) | 27건 · **위반 2** |
| R3 | 같은 출처를 가리키는 값끼리 같은가 | 14건 · 위반 0 |
| R4 | 눈금(SCALE) 여섯 칸이 한 런에서 오는가 | 6건 · 위반 0 |
| R5 | 값이 있으면 값 라벨 · 없으면 '이 시기 미수집' | 96건 · 위반 0 |
| R6 | `BNECK_SYNC_SPEC.md` 와 값이 같은가 | 75건 · **위반 28** |
| R7 | 하드코딩된 개수 문구가 실제 카드 수와 맞는가 | 47건 · **위반 11** |

- **검사 대상이 0 건인 규칙은 통과가 아니라 경고**로 찍는다(조용히 0건 통과하면 검사기가 죽은 걸 모른다).
- 규칙마다 **회귀 시험**을 붙여 매 실행마다 돌린다 — 옛 결함(⑩ 출처 없는 값 · ⑭ 곱셈 어긋남 ·
  ⑮ 소켓 135.2k · 눈금 셋만 7,200 · ⑫ 미수집 라벨 · 명세 불일치 · '열두 장' · '실험 12건')을
  다시 넣어 규칙이 잡는지 본다. **8/8 통과.**

### 첫 실행 결과 — 위반 41건, 셋 다 주우진 판단 대기 (고치지 않았다)

1. **R2 2건** — ⑤ `2,100,000 × 19.7 = 41.4` 인데 송신량 칸은 `41` (+0.90%) · ⑧ `8,710,000 × 71 = 618.4`
   인데 `614` (+0.72%). 둘 다 노션 표기의 반올림 차로 보인다(⑤ 는 애초에 환산값). 값을 맞출지,
   문턱을 1% 로 올릴지는 판단할 자리.
2. **R6 28건** — 전부 **명세(08-18 채집) 이후에 정정한 값**이다(재대조 5건 + P7 4건). 예: 현황판이
   5,599 트래픽에서 7,200 런으로, ⑭⑮ 의 틱·소켓을 CSV 에서 채운 것. 명세를 지금 값으로 갱신하거나,
   검사기에 '명세 이후 정정' 예외표를 두거나 둘 중 하나가 필요하다. **지금은 명세가 정본이라 전부 위반으로 뜬다.**
3. **R7 11건** — `bneck.css`·`bneck-tail.css`·`common.css` 에 12장·15건 시절 문구가 남아 있다
   (P4 는 `bneck.js`·`scene.js`·`panel-bneck.html` 셋만 손댔다). '한때·그때·날짜' 가 붙은 12줄은
   시점을 박아 둔 서술이라 참고로만 찍는다.

**참고 1건** — ⑦ 끄고 틱 `66.4` 는 출처로 적은 런(`C4000_WT12_K2_r3`)의 값 `59.55` 와 11.5% 벌어진다.
3회 **평균**(66.35)과는 맞는다 — 같은 카드 안에서 트래픽은 중앙값 런, 틱은 3회 평균을 쓰고 있다.
값이 틀린 게 아니라 축이 섞인 것이라 그대로 두고 기록만 한다.

### 검사기가 안 보는 것 (정직)

- `ab` 표(실험별 지표)는 R6 범위 밖이다 — 서술과 값이 섞여 있어 기계 대조가 안 선다.
- 화면 렌더는 안 본다. 라벨·눈금은 `bneck.js` 소스를 정규식으로 확인할 뿐이라, 그쪽 구조가 바뀌면
  R0 이 먼저 운다(그게 R0 을 둔 이유다).
- `index.html` 은 이번에 안 만들었다 — `src` 는 화면에 안 나오는 데이터라 다음 빌드 때 따라오면 된다.
